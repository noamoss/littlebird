<?php
/**
 * @file
 * Module file for Rest EASY API.
 */

/**
 * Implements hook_ctools_plugin_directory().
 */
function littlrbird_api($module, $plugin) {
  if ($module == 'restful') {
    return 'plugins/' . $plugin;
  }
}
